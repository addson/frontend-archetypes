
/* 16 Arrays */
console.log("");

// for in
var letras = new Array("A", "B", "C", "D", "E");
console.log(letras);
for (const indice in letras) {
    console.log("elemento do array: " + letras[indice]);
}

// for of
var numeros = [1, 2, 3, 4, 5];
console.log(numeros);
for (const numero of numeros) {
    console.log("elemento do array: " + numero);
}

console.log();










